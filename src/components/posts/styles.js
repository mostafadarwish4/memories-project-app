import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  main: {
    display: 'flex',
    alignItems: 'center',
    
  },
  smMargin: {
    margin: "1rem",
  },
  actionDiv: {
    textAlign: 'center',
  },
  
}));